# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

# This has to be the same as the target in the xcode project
target 'gameNewsV2' do
   use_frameworks!
  # Pods for gameNews
  pod 'Alamofire'
  pod 'Kingfisher'
end
